# Managed by site-deploy host-converge — edits on the box are overwritten each tick.
# 10, not the default 60: a serving box's hot set is page cache, which is cheap
# to re-read, so evicting the app's anonymous memory to hold more of it is
# precisely backwards.
vm.swappiness = 10
